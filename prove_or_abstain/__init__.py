"""prove_or_abstain — deterministic causal-investigation pipeline.

Core package: state (agent_state), math (metrics, attribution), decision
(gates), orchestration (nodes, graph), LLM boundary (llm), data sources
(connectors), and side effects (autopilot, webhook).
"""
