# Secrets — must NEVER enter the image.
.env

# Local environments / caches
.venv
**/__pycache__
**/*.pyc
.pytest_cache/
.DS_Store

# Not needed at API runtime (synthetic panels are in-memory; examples/ IS
# needed — benchmark.py and /investigate/example read its real-data CSVs)
data/
tests/
scripts/
.github/
.git/
.claude/
.gitignore
Dockerfile
.dockerignore
